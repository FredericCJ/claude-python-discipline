# Codex entry point

Read `CLAUDE.md` in full before working in this repository. Despite its historical
filename, it is the shared maintainer guide for Claude Code and Codex: it defines the
discipline entry point, generated-file rules, gate, environment, and release constraints.

`skills/python-discipline/` is the authored skill. The copies under `.claude/skills/` and
`.agents/skills/` are generated host discovery surfaces; never edit either copy directly.
