# Exact Python dependencies for the vendored tools and canonical project gate.
#
#     pip install -r .agent/requirements.txt
#
# This is the ADOPTER manifest. The maintainer lock is `environment.yml`, but a
# consuming repository runs these verifier adapters against its own code. A
# missing tool is an explicit red/unsupported outcome, never a narrower green gate.
#
# Python 3.11 or newer (discipline/fact/py-typing.md).

PyYAML==6.0.3
jsonschema==4.26.0

# Static, typing, import-boundary, test-control, build, mutation, and generated-doc
# adapters. Each is exact because its diagnostics and exit semantics are evidence.
ruff==0.16.3
mypy==2.3.0
pyright==1.1.407
import-linter==2.13
pytest==9.1.1
pytest-timeout==2.4.0
pytest-randomly==4.1.0
pytest-socket==0.8.0
build==1.3.0
cosmic-ray==8.7.0

# NOT required, and deliberately not listed as such:
#
#   tools/learn.py       standard library only
#   tools/integrate.py   standard library only
#   tools/vendor.py      standard library only
#
# Those three announce, learn, and update, so package lifecycle operations
# themselves remain standard-library-only.
#
# tiktoken is NOT required. tools/build_index.py measures the `tokens:` field
# with a character-ratio estimate defined in tools/discipline_core.py, so its
# output is the same on every machine. An earlier version imported tiktoken when
# present and estimated when absent, which made generated bytes depend on what
# happened to be installed.
